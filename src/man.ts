import * as Pring from 'pring'
import * as FirebaseFirestore from '@google-cloud/firestore'
import {
    firestore,
    SKUProtocol,
    OrderItemProtocol,
    ProductProtocol,
    OrderProtocol,
    TransactionProtocol,
    AccountProtocol,
    StockType,
    StockValue,
    OrderStatus,
    PaymentDelegate,
    PaymentOptions,
    RefundOptions,
    Currency,
    TransactionType,
    Balance,
    TransferOptions
} from "./index"

export class Man {
    
}